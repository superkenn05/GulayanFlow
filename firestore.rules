rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    /**
     * GEMMA'S GULAYAN - SECURITY RULES
     * 
     * Hierarchy: Superadmin (markken@gulayan.ph) > Admin > Staff
     */

    // --- Helper Functions ---

    function isSignedIn() {
      return request.auth != null;
    }

    function getStaffRole() {
      let path = /databases/$(database)/documents/staffUsers/$(request.auth.uid);
      return exists(path) ? get(path).data.role : 'Guest';
    }

    function isSuperadmin() {
      // The email is the ultimate source of truth for the single Superadmin
      return isSignedIn() && request.auth.token.email == 'markken@gulayan.ph';
    }

    function isAdmin() {
      let role = getStaffRole();
      return isSignedIn() && (role == 'Admin' || isSuperadmin());
    }

    function isStaff() {
      let role = getStaffRole();
      return isSignedIn() && (role == 'Staff' || isAdmin());
    }

    // --- Collection Rules ---

    match /staffUsers/{staffUserId} {
      // Users can always read/update their own basic profile
      allow get, update: if isSignedIn() && request.auth.uid == staffUserId;
      
      // Only the Superadmin can manage the directory and assign roles
      allow read, write: if isSuperadmin();
      
      // Admins can view the directory but not modify it
      allow list, get: if isAdmin();
    }

    match /categories/{categoryId} {
      allow read: if isSignedIn();
      allow write: if isStaff();
    }

    match /products/{productId} {
      allow read: if isSignedIn();
      allow write: if isStaff();
    }

    match /stockTransactions/{transactionId} {
      allow read: if isStaff();
      allow create: if isStaff();
      allow update, delete: if isAdmin();
    }

    // Global deny for everything else
    match /{document=**} {
      allow read, write: if false;
    }
  }
}