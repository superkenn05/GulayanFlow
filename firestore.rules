rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Explicitly allow all authenticated users to read and write
    // to any collection. This is the "Clean Version" for MVP reliability.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}